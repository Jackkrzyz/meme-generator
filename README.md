1. Run this command in the project's root directory:
```
npm i
```
2. Create a .env file in the project's root directory
```
OPENAI_API_KEY=YOUR_API_KEY
PORT=OPEN_PORT (ex: 8080)
```
3. Run this command in the project's root directory:
```
npm run start
```